import { StyleSheet } from "react-native";

const styles = StyleSheet.create({

    container: {
        padding: 24
    },

    userImageContainer: {
        width: "100%",
        alignItems: "center"
    },

    userImage: {
        width: 156,
        height: 156
    }

})

export default styles